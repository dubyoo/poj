#include <iostream>

using namespace std;

int D[200];
int P[200];

int test()
{
	int n, m;
	while (cin >> n >> m && n + m)
	{
		for (int i = 0; i < n; i++)
			cin >> D[i] >> P[i];


	}
	return 0;
}

// unfinished
